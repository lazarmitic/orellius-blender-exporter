# Documentation
#
# mesh [mesh_name] // represents start of mesh object
# vertexData [x, y, z, s, t, nx, ny, nz, ...] // represents vertex data in format same as VBO format that we send to GPU
# indiceData [a, b, c, a, b, c, ...] // tells GPU how to connect vertices to form triangles
# diffuseMap [url] // represents mesh diffuse map
# normalMap [url] // represents mesh normal map
# specularMap [url] // represents mesh specular map
# ambientMap [url] // represents mesh ambient map
